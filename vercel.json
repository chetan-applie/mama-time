{
  "version": 2,
  "buildCommand": "npm run setup -- --production=1 --base-url=https://mama-time-frontend.vercel.app --admin-email=admin@sentinators.local --force=1 && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/((?!api).*)*",
      "destination": "/index.html"
    }
  ]
}