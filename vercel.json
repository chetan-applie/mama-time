{
  "version": 2,
  "buildCommand": "npm run build && npm run doctor",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/((?!api).*)*",
      "destination": "/index.html"
    }
  ]
}