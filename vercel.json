{
  "version": 2,
  "buildCommand": "npm run setup -- --production=1 --force=1 && npm run db:migrate && npm run build && npm run doctor",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/((?!api).*)*",
      "destination": "/index.html"
    }
  ]
}