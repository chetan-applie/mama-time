{
  "version": 2,
  "buildCommand": "npm run setup -- --production=1 --base-url=https://Your-domain.name/ --force=1 && npm run db:migrate && npm run build && npm start",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/((?!api).*)*",
      "destination": "/index.html"
    }
  ]
}